{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubearchinspect.serviceAccountName" . }}
  namespace: {{ include "kubearchinspect.namespace" . }}
  labels:
    {{- include "kubearchinspect.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
